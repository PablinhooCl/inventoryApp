extends layout

block content

  h1 #{title}: #{itemInstance.name}
    p Do you really want to delete this Instance?

    form(method='POST')
      div.form-group
        input#itemInstanceid.form-control(type='hidden', name='itemInstanceid', value=itemInstance._id )

      button.btn.btn-primary(type='submit') Delete